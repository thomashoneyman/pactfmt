{
  "name": "docs",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "cd src/wasm && wasm-pack build --target web --out-dir ../../pkg",
    "start": "npx serve ."
  }
}
